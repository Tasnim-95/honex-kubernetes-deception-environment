
server:
    port: 8081
spring:
  datasource:
    url: jdbc:mysql://mysql-employee:3306/employee_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

